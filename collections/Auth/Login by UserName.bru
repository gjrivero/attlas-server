meta {
  name: Login by UserName
  type: http
  seq: 1
}

post {
  url: {{hostApi}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "Master_Key."
  }
}

script:post-response {
  bru.setEnvVar("token",res.body.token);
  bru.setEnvVar("refresh_token",res.body.refresh_token);
  bru.setEnvVar("csrf_token",res.body.csrf_token);
}
